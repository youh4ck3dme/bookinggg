# TODO

- Implement real API adapters, Prisma schema, and provider registry wiring.
- Replace local-only booking creation with live API calls and error handling.
- Add real validation with server responses and loading skeletons.
- Add icons for PWA manifest and widget build pipeline (bundler).
- Expand tests for API idempotency and web critical flows.
